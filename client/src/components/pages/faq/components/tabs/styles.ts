import { cn } from '@/utils';

export const getStyles = () => ({
  wrapper: 'flex w-full flex-col px-4 tablet:flex-row tablet:px-0',
  btn: cn('h-[44px] w-full rounded-t-[32px] text-center font-scada text-[20px] uppercase'),
});
