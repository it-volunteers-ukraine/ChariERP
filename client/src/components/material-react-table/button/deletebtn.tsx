/* const CustomDeleteButton = () => (
  <button
    style={{
      position: 'absolute',
      top: 10,
      right: 10,
      zIndex: 1000,
      background: 'transparent',
      border: 'none',
      cursor: 'pointer',
      fontSize: '24px',
      color: 'red',
    }}
    aria-label="Видалити фото"
    onClick={() => {
      onDeletePhoto(slides[currentIndex].src);
      setOpen(false);
    }}
  >
    🗑️
  </button>
);
 */
