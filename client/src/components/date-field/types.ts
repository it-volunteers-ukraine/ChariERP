export interface DateFieldProps {
  name: string;
  label: string;
  className?: string;
  readonly?: boolean;
  required?: boolean;
  placeholder: string;
  wrapperClass?: string;
  placeholderItalic?: boolean;
}
