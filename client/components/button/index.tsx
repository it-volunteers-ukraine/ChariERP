import { Icon } from '@/assets';
export const Button = ({}) => {
  return (
    <>
      <span className="p-8 transition-all duration-1000 bg-bottom bg-200 bg-btn-primary hover:bg-top hover:border hover:border-red-500">
        <Icon.UA />
      </span>
    </>
  );
};
